export enum JobStatus {
  ACTIVE = 'active',
  UNLISTED = 'unlisted',
  EXPIRED = 'expired',
  BLOCKED = 'blocked',
  CLOSED = 'closed',
}

export enum JobClosureType {
  AUTO_FILLED = 'auto_filled',
  MANUAL = 'manual',
}
